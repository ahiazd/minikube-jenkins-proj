pipeline {
  agent { dockerfile true}
   stages {
    stage ('BUILD') {
     steps {
         set -x
         echo 'running zip_job script'
         mkdir scripts
         cp /tmp/zip_job.sh ./scripts
#         chmod +x scripts/zip_job.sh
         ./scripts/zip_job.sh
       }
      }
    }
  }
