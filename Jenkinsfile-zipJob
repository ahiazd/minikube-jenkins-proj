pipeline {
  agent { dockerfile true}
   stages {
    stage ('BUILD') {
     steps {
         pwd
         echo 'running zip_job script'
         mkdir scripts
         ls  scripts/zip_job.sh
         cp /tmp/zip_job.sh ./scripts
         scripts/zip_job.sh
       }
      }
    }
  }
