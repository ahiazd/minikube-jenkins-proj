pipeline {
   agent { label 'master'}
    stages {
        stage('test') {
            steps {
                sh 'echo "hello"'
            }
        }
      }
      agent { dockerfile true}
    stages {
        stage('test-agent') {
            steps {
                sh 'echo "hello"'
                }
          }
       } 
      
    }
