pipeline {
  agent { 'dockerfile true' }
         stages {
           stage ('Test') {
            steps {
           sh '''
           echo "Hello Test"
           cat /etc/release-os
              '''
          }
         }
       }        
     }
   

