pipeline {
    agent { 
     docker { image ‘dockerfile:latest’ }
    stages {
        stage('test') {
            steps {
                sh 'echo "hello"'
            }
           }
         }
      }
   }
