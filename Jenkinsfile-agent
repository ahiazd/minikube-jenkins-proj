pipeline {
 agent { label 'master' }
  stages { 
   stage ('pre run') {
    steps {
     sh 'echo "pre run"'
      }
     }
  agent { dockerfile true}
    stage ('Test') {
     steps {
      sh '''
       echo " dockerfile runs as an agent now... "
       '''
       }
      }
    }
  }
   
