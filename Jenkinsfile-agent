pipeline {
 agent any
  stages { 
   stage ('pre run') {
    steps {
     sh 'echo "pre run"'
      }
     }
  agent { label 'master' }
    stage ('Test') {
     steps {
      sh '''
       echo " dockerfile runs as an agent now... "
       '''
       }
      }
    }
  }
   
