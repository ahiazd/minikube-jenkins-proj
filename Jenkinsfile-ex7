podTemplate(containers: [
    containerTemplate(name: 'centos', image: 'ahiaz:latest', command: 'sleep', args: '99d'),
    
  ]) {

    node(POD_LABEL) {
        stage('Hello Python') {
            container('centos') {
                stage('Run zip script') {
                    sh '''
                    echo "This is a scripted pipeline running as an agnet... "                  
                    '''
                    
                }
            }
        }
    }
        
}
